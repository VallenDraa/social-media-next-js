export type User = {
  id: string;
  profilePicture: string;
  username: string;
  email: string;
  createdAt: string;
  updatedAt: string;
};

export type UserProfile = {
  id: string;
  username: string;
  profilePicture: string;
  createdAt: string;
};
